# frontend/public/

Files here are served at the site root by Vite.

## logo.png (pending)

The topbar brand mark and the favicon load from `/logo.png`. Drop the app-icon
image (pastel lake + volcano illustration, from the design chat) here as
`logo.png` — ideally a square PNG, 128–512 px. Until it exists, `Header.jsx`
renders an inline SVG fallback mark and the favicon 404s harmlessly.
