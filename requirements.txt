fastapi
uvicorn[standard]
pydantic
python-dotenv
httpx

# feature pipeline
sentinelhub
cdsapi
xarray
rasterio
netCDF4
numpy
pandas

# model
# Pinned: the committed src/model/artifacts/*.joblib are pickled with this
# version and fail to unpickle on a mismatched one (sklearn moves internal
# modules between minors). Bump this and the artifacts together.
scikit-learn==1.7.2
# Pinned: metrics.json publishes MAE as a reproducible claim, and
# src/model/per_pixel.py seeds torch's RNG directly (manual_seed + a seeded
# Generator for the DataLoader). Initialisation and optimiser internals shift
# across torch minors, so an unpinned bump changes the reported number from an
# unchanged command. Unlike scikit-learn above this is NOT an unpickling
# constraint: quantile_mlp.pt holds a state_dict and nothing loads it yet.
# Bump this and re-run the training together.
torch==2.14.0
joblib

# tests (GATE-TEST / GATE-MODEL)
pytest
