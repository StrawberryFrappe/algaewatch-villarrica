[pytest]
# `pythonpath` lets tests import `src.*` without a package install or a
# sys.path hack in every file. The project is run from its root (see
# scripts/train_model.py), so the root is the import root.
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers --strict-config
